{{- $pctx := . -}}
{{- $pages := slice -}}
{{- if .IsHome -}}
  {{- $pctx = .Site -}}
  {{- $pages = where (where (where .Site.Pages "Section" "in" site.Params.mainSections) "Parent.Kind" "ne" "home") "Parent.Parent.Kind" "ne" "section" -}}
{{- else if $.IsSection -}}
  {{- $pages = where $pctx.Pages "Parent.Kind" "ne" "home" -}}
  {{- with $pages -}}
    {{- $pages = where $pages "Parent.Parent.Kind" "ne" "section" -}}
  {{- end -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq (partial "page-title" .) (partial "site-title" .) }}(partial "site-title" .){{ else }}{{ with (partial "page-title" .) }}{{.}} on {{ end }}{{partial "site-title" . }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne (partial "page-title" .) (partial "site-title" .) }}{{ with (partial "page-title" .) }}in {{.}} {{ end }}{{ end }}on {{ (partial "site-title" .) }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    {{ range $pages }}
    <item>
      <title>{{ partial "page-title" . }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ if or (not .Params.author) (eq .Params.author .Site.Author.name) }}{{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>{{ .Description | default .Params.description | default .Params.summary | default .Summary | html }}</description>
    </item>
    {{ end }}
  </channel>
</rss>